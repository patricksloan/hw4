<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
  
  <!-- Bootstrap Icons -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet">

  <!-- Custom CSS -->
  <link rel="stylesheet" href="/stylesheets/application.css">

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>

  <title>Travel Journey</title>
</head>

<body>
  <!-- Bootstrap Navbar -->
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
      <a class="navbar-brand" href="/">Travel Journey</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
        aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav me-auto">
          <li class="nav-item">
            <a class="nav-link" href="/">Home</a>
          </li>
        </ul>
        
        <ul class="navbar-nav">
          <% @user = User.find_by({ "id" => session["user_id"] }) %>
          <% if @user == nil %>
            <li class="nav-item"><a href="/users/new" class="nav-link">Sign Up</a></li>
            <li class="nav-item"><a href="/login" class="nav-link">Login</a></li>
          <% else %>
            <li class="nav-text p-2">Logged in as: <strong><%= @user["username"] %></strong></li>
            <li class="nav-item"><a href="/logout" class="nav-link">Logout</a></li>
          <% end %>

          <%# Button for creating new entry (Bootstrap style) %>
          <li class="nav-item ms-3">
            <a href="/entries/new" class="btn btn-success">New Entry</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Bootstrap container for page content -->
  <div class="container mt-3">
    <%# Flash messages (Bootstrap alert) %>
    <% if flash["notice"] != nil %>
      <div class="alert alert-primary">
        <%= flash["notice"] %>
      </div>
    <% end %>

    <%= yield %>
  </div>

  <!-- Bootstrap JS (included at bottom for performance) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
</body>
</html>
